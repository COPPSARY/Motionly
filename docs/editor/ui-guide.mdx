# Motionly UI Guide

Motionly uses one focused editor screen.

## Top Toolbar

- Open a `.motion` project
- Save the current project
- Export MP4 with progress feedback
- Open and star the GitHub repository

## Scene

The left panel lists project layers with type icons, media thumbnails, short descriptions, and their first visible time.

- Click a layer to select it and seek to its first visible frame.
- Click Text to add a text layer.

## Preview

The center panel renders the scene at the project's aspect ratio.

- Click an object to select it.
- Drag a selected object to move it.
- Drag corner handles to scale it.
- Click empty space to deselect.
- Use Fit, zoom, and fullscreen to inspect the canvas.

## Properties

The right panel edits the selected layer without requiring source changes.

- Position, scale, width, rotation, and opacity
- Text content, font size, and color
- Scene background color
- Animation preset, duration, delay, and easing

## Timeline

The lower panel contains playback and clip timing.

- Reset, Play/Pause, timecode, and frame display
- Scrubbable ruler and playhead
- One clip row per scene, text, media, or effect layer
- Media thumbnails and text labels inside clips
- Start and end trim handles
- Resizable panel height
- Audio attachment and audio timeline row
- Delete selected layer and preview zoom controls

## `.motion` Source

The `.motion` button opens the generated source for inspection. Visual editing remains the primary workflow.
