# Motionly composition starter

Copy `composition.ts` into `src/compositions/`, adjust its import paths if needed, and mount the exported definition with `CompositionRuntime`.

The composition builds semantic DOM directly and adds every animation to the provided GSAP timeline. Keep selectable elements registered with stable ids.
